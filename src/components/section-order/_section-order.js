/*
	секция Заявка
	--------------------------------------
	использованные библиотеки:
 	-------------------------------------- */
function initOrder() {
	Vue.component("app-order", {
		props: {
			Index: Number
		},
		data() {
			return {
			}
		},
		mounted() {
			// console.log("mounted", this.Index)
		}
	});
}